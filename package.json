{
  "name": "node-go-addon-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node .",
    "build": "cd ./lib && go build -buildmode=c-archive -o libgo.a && cd .. && node-gyp rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "node-gyp": "^9.1.0"
  },
  "author": "",
  "license": "ISC"
}
